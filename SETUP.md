# دليل تشغيل Sawty (إيجنت صوتي بعامية مصرية)

الدليل ده بيشرح خطوة بخطوة إزاي تشغّل المنصة فعليًا مع عملائك. اتقرا بصراحة تامة:
**المنصة نفسها مفتوحة ومجانية، لكن تشغيلها الفعلي بيعتمد على 2-3 خدمات خارجية
بتحسب حسب الاستخدام.** مفيش حل صوتي بجودة حقيقية 100% ببلاش دلوقتي — لكن كل
الخدمات اللي هنستخدمها بتديك رصيد تجريبي مجاني يكفي تجرب بيه كذا عميل قبل ما
تدفع أي حاجة فعلية.

## 1) Twilio — رقم التليفون الأمريكي

1. اعمل حساب على [twilio.com](https://www.twilio.com/try-twilio) (مجاني).
2. من الـ Console هياديك رقم أمريكي تجريبي تلقائي، أو اختار رقم من
   Phone Numbers → Buy a Number (فلتر Country = US).
3. من نفس الـ Console خد **Account SID** و **Auth Token** (في الصفحة الرئيسية).
4. **مهم بخصوص الحساب التجريبي (Trial):** الحساب التجريبي بيسمح لأي حد
   يتصل على رقمك عادي، لكن Twilio ممكن تشغّل رسالة ترويجية قصيرة قبل ما
   المكالمة توصل، وفيه سقف لرصيد المكالمات المجاني. لو عايز تجربة نضيفة
   تمامًا لعملائك الحقيقيين، هتحتاج تشحن رصيد بسيط وتـ"upgrade" الحساب —
   ده مش إلزامي للتجربة، بس إلزامي قبل ما تشتغل بيه فعليًا مع عملاء بيدفعولك.

## 2) OpenAI — الفهم والرد (وبديل توليد الصوت)

1. اعمل حساب على [platform.openai.com](https://platform.openai.com).
2. ضيف رصيد بسيط (بيتحاسب بالاستخدام — كل مكالمة بتكلف قروش قليلة جدًا).
3. من API keys اعمل key جديد وحطه في `OPENAI_API_KEY`.

## 3) ElevenLabs — صوت مصري طبيعي (اختياري لكن موصى بيه بشدة)

بدون ElevenLabs، هيرجع الـ Agent لصوت OpenAI الافتراضي اللي مش مخصص للهجة
المصرية بنفس الجودة. مع ElevenLabs بتقدر تختار أو تستنسخ صوت عربي طبيعي جدًا.

1. اعمل حساب على [elevenlabs.io](https://elevenlabs.io) (فيه باقة مجانية
   بعدد كلمات محدود شهريًا).
2. من مكتبة الأصوات (Voice Library) اختار صوت عربي، أو ارفع تسجيل صوتك/صوت
   بتاع حد موافق واعمل Voice Clone.
3. خد الـ API key والـ Voice ID وحطهم في `ELEVENLABS_API_KEY` و
   `ELEVENLABS_VOICE_ID`.

## 4) Resend — إرسال إيميل الأوردر لصاحب البراند (اختياري)

بدون Resend، الأوردرات هتفضل تظهر في لوحة التحكم بس مش هتتبعت إيميل.

1. اعمل حساب على [resend.com](https://resend.com) (فيه باقة مجانية).
2. اعمل API key وحطه في `RESEND_API_KEY`.
3. حط إيميل الإرسال في `NOTIFY_FROM_EMAIL` (لازم تفعّل الدومين بتاعك في
   Resend، أو استخدم إيميل التجربة اللي بيديهولك).

## 5) النشر (Deploy) — مرة واحدة بس

الكود بالفعل على GitHub (`boody172/arman`, براتش `main`). خطوة واحدة يدوية
لازم تعملها إنت بنفسك (محتاجة تسجيل دخول بحسابك على Vercel، محدش تاني يقدر
يعملها بدالك):

1. روح [vercel.com/new](https://vercel.com/new) وسجّل دخول.
2. Import Git Repository → اختار `boody172/arman`.
3. قبل ما تدوس Deploy، افتح Environment Variables وضيف كل المتغيرات اللي في
   `.env.example` (على الأقل `OPENAI_API_KEY`, `TWILIO_ACCOUNT_SID`,
   `TWILIO_AUTH_TOKEN`, و`DASHBOARD_ADMIN_TOKEN` = أي كلمة سر قوية من عندك).
4. دوس Deploy. بعد ما يخلص، خد الدومين اللي هيديهولك (`....vercel.app`) وضيفه
   كـ `APP_BASE_URL` في نفس الإعدادات، وأعد النشر مرة تانية (Redeploy) عشان
   يتفعّل.

من دلوقتي، **أي push جديد على براتش `main` هيعمل Deploy تلقائي**. يعني أي
مرة أضيفلك عميل جديد وأعمل push، الموقع هيتحدث لوحده من غير ما تلمس Vercel
تاني.

> ملاحظة أمان: مفاتيح زي `OPENAI_API_KEY` و`TWILIO_AUTH_TOKEN` تفضل بس في
> إعدادات Vercel — مش محتاج تبعتهالي أنا خالص، أنا مش بستخدمها ولا محتاجها
> في شغلي (إضافة العملاء بتتم في الكود نفسه، مش عن طريق الموقع اللايف).

## 6) اربط رقم Twilio الأول

1. من Twilio Console → Phone Numbers → اختار الرقم → Voice Configuration →
   "A call comes in" → Webhook، والصق:
   ```
   https://<الدومين بتاعك على Vercel>/api/twilio/voice
   ```
   Method = POST → Save. **نفس اللينك ده بيتستخدم لكل أرقام العملاء** (الكود
   بيعرف يوجّه كل مكالمة للبراند الصح على حسب الرقم اللي اتصل عليه).

## 7) إزاي نضيف كل عميل جديد

أبسط طريقة: **ابعتلي في الشات** — اسم البراند، نوع النشاط، إيميل صاحبه،
المنيو والأسعار، لينكات الانستجرام/الفيسبوك/الموقع، ورقم Twilio بتاعه (لو
لسه معملتلوش رقم، قوللي وأنا هقولّك تشتري رقم جديد من Twilio له). وأنا:

1. بضيف بيانات العميل في `data/brands.ts`.
2. بعمل commit و push على `main`.
3. Vercel بتعمل Deploy تلقائي — العميل بيبقى شغّال حي خلال دقيقة أو اتنين.

⚠️ **حاجة مهمة تعرفها عن التكلفة:** كل عميل محتاج **رقم Twilio منفصل خاص
بيه** (مش ممكن رقم واحد يخدم أكتر من براند)، فالرقم المجاني التجريبي بيغطي
عميل واحد بس — أي عميل بعده هتحتاج تشتري له رقم من Twilio (تكلفة بسيطة
شهرية تقريبًا دولار وحاجة + تكلفة المكالمات).

لو عايز تضيف عميل بنفسك من غير ما تستنى عليّ، فيه فورم في `/dashboard` —
لكن اقرا الملاحظة تحت، الفورم ده للتجربة المحلية بس ومش بيفضل شغال على
Vercel.

## 8) لو عايز رقم البراند القديم (المصري) يحوّل على رقم Twilio بتاعه

لو العميل عنده رقم منشور بالفعل (على واجهة المحل، منيو مطبوع، جوجل ماب...)
وعايز عملاؤه يفضلوا يكلموا نفس الرقم القديم بدل ما يتعلموا رقم جديد، الحل
هو **تحويل مكالمات (Call Forwarding) من الرقم المصري القديم لرقم Twilio
الأمريكي بتاع البراند ده**. الحاجة المهمة تعرفها: **ده إعداد بيتعمل من
شبكة المحمول نفسها، مش من الكود ولا من Twilio ولا مني** — أنا معنديش أي
تحكم فيه، والعميل (أو صاحب خط الرقم القديم) لازم يعمله بنفسه.

### إزاي يتعمل (كود GSM قياسي بيشتغل مع أغلب الشبكات المصرية)

من موبايل الرقم القديم بتاع البراند، يكلّم الكود ده (بيحل محل `[TWILIO_NUM]`
رقم Twilio بالصيغة الدولية كاملة زي `+1XXXXXXXXXX`):

- تحويل كل المكالمات دايمًا: `**21*[TWILIO_NUM]#` ثم اتصال
- تحويل لو الخط مشغول: `**67*[TWILIO_NUM]#`
- تحويل لو محدش رد: `**61*[TWILIO_NUM]#`
- تحويل لو الموبايل مقفول/خارج التغطية: `**62*[TWILIO_NUM]#`
- إلغاء كل التحويلات: `##002#`

الأكواد دي معيار GSM عالمي، وبتشتغل غالبًا مع فودافون/أورانج/اتصالات/We
المصرية، لكن ممكن تختلف حسب الباقة (خط مسبق الدفع/فوترة) — لو الكود مشتغلش
جرّب من تطبيق الشبكة نفسها (My Vodafone / My Orange / We...) أو اتصل
بخدمة عملاء الشبكة واطلب "تحويل مكالمات لرقم دولي".

### حاجتين مهمين قبل ما تنصح العميل بالطريقة دي

1. **التكلفة:** التحويل بيتحسب على إنه مكالمة دولية طالعة من الرقم المصري
   القديم لكل مكالمة بتتحول — يعني صاحب الرقم القديم بيدفع تعريفة مكالمة
   دولية لمصر → أمريكا على كل مكالمة عميل، طول ما الخدمة شغالة. ده ممكن
   يبقى مكلف لو حجم المكالمات كبير، فلازم العميل يسأل شبكته عن التعريفة
   الدولية بالظبط قبل ما يفعّلها.
2. **بديل أرخص وأبسط:** لو العميل لسه بيبدأ يعلن عن رقم طلبات جديد (مش رقم
   قديم متعوّد عليه ناس كتير)، أنصح إنه يستخدم رقم Twilio نفسه كرقم الطلبات
   الرسمي من الأول (يحطه في الانستجرام/الفيسبوك/جوجل ماب) — كده مفيش تحويل
   ولا تكلفة مكالمة دولية إضافية خالص.

من ناحية الكود: مفيش أي تعديل مطلوب — التحويل شفاف بالنسبة لـ Twilio، بيوصله
كأنه مكالمة عادية جاية على رقمه، فالـ webhook بيشتغل عادي زي أي مكالمة تانية.

## 9) جرّب

اتصل على رقم Twilio من موبايلك واسمع الرد. لو حصل أي خطأ، شوف الـ Logs في
Vercel (Functions → Logs) هيوريك السبب بالظبط (مفتاح ناقص، خطأ من أي API...).

## حدود مهمة تعرفها

- **البراندات دلوقتي بتتخزن في الكود نفسه (`data/brands.ts`)** عشان تفضل
  موجودة دايمًا على Vercel حتى لو السيرفر عمل "cold start". الفورم اللي في
  `/dashboard` بيضيف/يعدّل بس في ذاكرة مؤقتة، مفيدة للتجربة المحلية لكن
  بتتمسح مع أي إعادة نشر — أي عميل حقيقي لازم يتضاف في `data/brands.ts`
  (زي ما شرحنا فوق).
- **حالة المكالمة نفسها (الحوار جوه المكالمة) بقت stateless بالكامل** —
  Vercel بتنشر كل route كـ function منفصلة معندهاش ذاكرة مشتركة مع التانية،
  فبدل ما نخزن "الجلسة" على السيرفر، بنبعت تاريخ الحوار مشفّر جوه اللينك
  اللي Twilio بترجعله بعد كل تسجيل. ده معناه المكالمة بتشتغل صح على Vercel
  بغض النظر عن أي instance استقبل أي جزء منها.
- **الأوردرات** (اللي بتتسجل وقت المكالمة نفسها) لسه في ذاكرة مؤقتة لكل
  function — ده معناه لوحة "الأوردرات" في الداشبورد **مش مضمون تشوف كل
  الأوردرات على Vercel** (ممكن تشوف بعضها بس حسب أي instance استقبل الطلب).
  التوصيل الموثوق 100% هو الإيميل (Resend) لأنه بيتبعت وقت تسجيل الطلب نفسه
  ومش محتاج حد يقرأ من التخزين بعد كده. اعتبر لوحة الأوردرات "نظرة سريعة"
  بس، مش سجل رسمي، لحد ما نربط قاعدة بيانات حقيقية (Vercel Postgres/Supabase).
- **زمن الاستجابة:** كل دورة كلام (تسجيل → تحويل لنص → فهم ورد → توليد صوت)
  بتاخد ثواني معدودة، مش لحظية زي مكالمة بشرية 100%. لو عايز أقل زمن ممكن،
  الخطوة الجاية هي التبديل لـ OpenAI Realtime API بدل الـ pipeline الحالي.
- **الحساب الإداري:** فيه توكن واحد بيحمي لوحة التحكم كلها (مناسب لشخص واحد
  بيدير كل عملائه). لو هيشتغل بيه أكتر من حد، محتاج نظام صلاحيات حقيقي.
